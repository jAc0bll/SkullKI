# Skull King AI — Python dependencies
# Install: pip install -r requirements.txt
#
# Note: gymnasium must be <1.3.0 because stable-baselines3 requires it.
# Installing stable-baselines3 first will pull the correct version automatically.

# ── Core game (no external deps) ─────────────────────────────────────────────
# skull_king/ uses only the standard library

# ── RL Training ───────────────────────────────────────────────────────────────
numpy>=1.24
torch>=2.0
gymnasium>=0.29,<1.3.0
stable-baselines3>=2.3
sb3-contrib>=2.3

# ── Config ────────────────────────────────────────────────────────────────────
PyYAML>=6.0

# ── Visualization (tournament plots + TensorBoard training monitor) ───────────
matplotlib>=3.7
tensorboard>=2.14
tqdm>=4.66
rich>=13.0

# ── Web interface ─────────────────────────────────────────────────────────────
fastapi>=0.100
uvicorn[standard]>=0.20

# ── Development / testing ─────────────────────────────────────────────────────
pytest>=8.0
pytest-cov>=5.0
